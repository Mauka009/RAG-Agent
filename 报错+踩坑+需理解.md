openai.OpenAIError: Missing credentials. 
调用的时候读不到我的API Key。在.env里面写的是API_KEY="SK-XXX"，前面应该是OPEN_API_KEY
from dotenv import load_dotenv
load_dotenv()要放在第一行或最前面

RuntimeError: Error loading ./ribao1/docs.txt
因为把存文档的文件夹名字改成docs,代码找不到文件了

NameError: name 'OPENAI_API_KEY' is not defined
在写"Authorization": f"Bearer {OPENAI_API_KEY}"的时候，没有在前面定义，应该先定义。api_key = os.getenv("OPENAI_API_KEY")。因为是siliconflow的API，改成SILICONFLOW_API_KEY作为区分。

 exceptions.clear()  # raise only the last error
 模型下载失败，用vpn后成功

 for user_msg, bot_msg in history:   # 非要一次抓 2 个
 因为假设给的数据长度是固定的，但Gradio升级后会自动往后面赛额外字段，会导致报错：too many values to unpack (expected 2
 改为按索引取值，忽略多余字段
 # 原代码（第 9.3 节）
for user_msg, bot_msg in history:
    messages.append({"role": "user", "content": user_msg})
    messages.append({"role": "assistant", "content": bot_msg})

# 修改为：
for turn in history:
    if len(turn) >= 2:
        user_msg = turn[0]
        bot_msg = turn[1]
        messages.append({"role": "user", "content": user_msg})
        messages.append({"role": "assistant", "content": bot_msg})
    else:
        # 如果格式异常，可以选择跳过或记录日志
        print(f"⚠️ 跳过异常历史项: {turn}")
# 修改后第二轮对话返回 "❌ 系统异常: 0"
修改为：        # ---------- 9.3 构建消息列表（包含历史）- 升级版 ----------
        messages = [{"role": "system", "content": system_prompt}]

        for turn in history:
            # 情况1：如果是字典（Gradio 新版格式）
            if isinstance(turn, dict):
                # 直接按角色添加（有 role 和 content 字段）
                if "role" in turn and "content" in turn:
                    messages.append({"role": turn["role"], "content": turn["content"]})
                # 万一字典里存的是 {"user": "...", "bot": "..."} 这种冷门格式，兜底处理
                elif "user" in turn and "bot" in turn:
                    messages.append({"role": "user", "content": turn["user"]})
                    messages.append({"role": "assistant", "content": turn["bot"]})
            
            # 情况2：如果是列表或元组（传统格式）
            elif isinstance(turn, (list, tuple)):
                if len(turn) >= 2:
                    messages.append({"role": "user", "content": turn[0]})
                    messages.append({"role": "assistant", "content": turn[1]})
                elif len(turn) == 1:
                    # 只有用户消息，没有机器人回复（异常情况，但保住不崩溃）
                    messages.append({"role": "user", "content": turn[0]})
            
            # 情况3：未知格式，打印警告但绝对不崩溃
            else:
                print(f"⚠️ 未知的历史格式，已跳过: {turn}")

        # 最后加上当前用户的问题（这一行在 for 循环外面，但还在 try 里面）
        messages.append({"role": "user", "content": question})

#最后加上当前用户的问题
messages.append({"role": "user", "content": question})
这样设计 ask_rag 函数，没有对 Gradio 传入的 history 数据结构做任何硬编码假设。通过 isinstance 做了运行时类型检查，分别适配了字典模式（新版 Gradio）和列表/元组模式（旧版 Gradio）。这种设计模式叫适配器模式（Adapter Pattern），它使得业务逻辑（调用大模型）与前端框架的版本变化彻底解耦，即便以后 Gradio 升级，核心代码也无需改动。


all-MiniLM-L6-v2这个模型总是连接失败，每次运行不开vpn都未响应一直重试连接，以为开了镜像也是这样，结果是因为写在了from sentence_transformers import CrossEncoder 后面，这句一优先执行就会导致还是去国外，然后就卡死了。把镜像站写在文件顶部就好了。

在config.py里读取要在里面写成X_XX = float/int （os.getenv（“ ”，“ ”））这种。写成os.getenv()不写死。这是为了如果在env里存了的话。env里面改成5的话，全局就算调用config.py里面的数据调的不是3是5。写第二个参数是为了如果env里面没有就用第二个参数。

把docs里的文档改成了多个文档，但是运行后向量库直接加载了以前的本地向量库。简历缓存重建机制，通过配置开关控制是否强制刷新向量库。

RateLimitError: Error code: 429 - {'message': 'Request was rejected due to rate limiting. Details: TPM limit reached.', 'data': None}每分钟 token 数超限了硅基流动对每个模型的 TPM 都有配额限制，请求频率太高，被限流
在 generate_with_chunks 调用时，添加 run_config 参数控制并发
from ragas.run_config import RunConfig

testset = generator.generate_with_chunks(
    chunks=chunks,
    testset_size=50,
    query_distribution=default_query_distribution,
    run_config=RunConfig(max_workers=4, max_retries=3)  # 限制并发数为4，重试3次
)